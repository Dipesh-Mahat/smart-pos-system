{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.8.3"
  },
  "scripts": {
    "start": "npx http-server public -p 3000",
    "build": "echo 'Static files ready for deployment'",
    "test": "echo 'No tests for static frontend'"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
